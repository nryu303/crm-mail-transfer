spring:
  datasource:
    url: jdbc:mysql://localhost:3306/crm_test?useSSL=false&characterEncoding=utf8&serverTimezone=Asia/Tokyo&allowPublicKeyRetrieval=true
    username: crm_user
    password: ${DB_PASSWORD:crm_user_dev_2026}
  flyway:
    clean-disabled: false

logging:
  level:
    com.crm: DEBUG
